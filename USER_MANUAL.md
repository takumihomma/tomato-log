# Tomato Log ユーザー操作マニュアル

Tomato Log（トマトログ）は、スマホやPCのローカル環境のみで安全にデータを管理できるライフログ PWA アプリです。
「設定 ⚙」ボタンからアクセスできる主要な4つの機能と使い方のマニュアルです。

---

## 1. 🛡️ 永続ストレージ設定

### 概要・目的
スマホやブラウザの空き容量が少なくなった際、OSやブラウザが保存データを自動消去してしまうのを防ぎ、データを端末内に永続保存するための保護機能です。

### 使い方
- **「保護を有効化する」と表示されている場合:**
  - タップしてストレージ保護の許可を与えます。
- **「永続化 有効中」と表示されている場合:**
  - 既に保護が有効化されています。特別な操作は不要です。

---

## 2. 📦 データ一括バックアップ

### 概要・目的
これまでアプリ内に保存した日付別の Markdown（.md）ログデータと、添付したすべての画像・ファイルを **1つの ZIP ファイルにまとめてダウンロード** する機能です。

### 使い方・手順
1. 「設定 ⚙」画面内の **「ZIPバックアップ出力」** ボタンをタップします。
2. データ圧縮の進捗状況ダイアログが表示されます。
3. 完了すると、端末に `tomato_log_backup_YYYY-MM-DD.zip` が自動保存されます。

---

## 3. 🔔 タイマー通知の許可

### 概要・目的
設定した時間間隔（15分、30分、1時間など）ごとに、ログの記録を促すアラーム通知をスマホやPCの画面に送信する許可設定です。

### 使い方・手順
1. 「設定 ⚙」画面内の **「通知を許可する」** ボタンをタップします。
2. ブラウザの許可ダイアログで **「許可」** を選択します。
3. トップページのタイマーで「タイマー開始」を押すと、指定時間ごとにリマインダー通知が届きます。

---

## 4. 📍 位置情報の自動添付

### 概要・目的
ログ保存時に現在地（緯度・経度）を取得し、Google Maps リンクを Markdown 内へ自動添付する機能の ON / OFF 設定です。

### 使い方・手順
1. 「設定 ⚙」画面内の **「位置情報添付 ON / OFF」** ボタンをタップして切り替えます。
2. ON の状態でログを追加すると、自動的に現在地マップリンクが Markdown に記録されます。

---

### お困りの場合（トラブルシューティング）
- **通知が届かない場合:** スマホ本体の「設定 ➔ 通知 ➔ ブラウザ（Chrome等）」の通知許可がオンになっているかご確認ください。
- **データについて:** すべてのデータは端末内部（IndexedDB）にのみ保存されています。外部サーバーへ送信されることはありません。
