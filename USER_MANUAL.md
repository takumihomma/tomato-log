# Tomato Log ユーザー操作マニュアル

Tomato Log（トマトログ）は、スマホやPCのローカル環境のみで安全にデータを管理できるライフログ PWA アプリです。
「設定 ⚙」ボタンからアクセスできる主要な4つの機能と使い方のマニュアルです。

---

## 🔊 定期タイマー & 女性音声通知機能

- 設定したタイマー時間（15分、30分、1時間など）が経過すると、スマホやPCから大きめの女性の声で **「What's up?」** と音声が自動発声され、画面にリマインダー通知が届きます。
- **「🔊 音声テスト」** ボタンを押すことで、事前に「What's up?」の女性音声を試聴できます。
- **💡 画面自動消灯防止（Screen Wake Lock）機能:**
  - タイマーが開始（カウントダウン）されると、ブラウザの Screen Wake Lock 機能が全自動で有効化され、**ポモドーロタイマー動作中にスマホの画面が自動的に暗くなってスリープに入るのを防止**します（タイトル横に「画面消灯防止 ON」バッジが表示されます）。タイマーを停止すると自動解除されます。
## 1. ⏰ 毎日自動起動スケジュール

### 概要・目的
毎日指定した開始時刻（例: `09:00`）と終了時刻（例: `18:00`）の範囲内でのみ、自動的に定期タイマーを開始・停止するスケジューラー設定です。

### 使い方
1. 「設定 ⚙」画面を開き、一番上の **「1. ⏰ 毎日自動起動スケジュール」** で「スケジュール ON」に切り替えます。
2. 開始時刻と終了時刻を指定します。
3. ON になっている間、指定時間帯に自動でタイマーが開始し、指定時間外になると自動で停止します。

---

## 2. 🛡️ 永続ストレージ設定

### 概要・目的
端末の空き容量不足時に、OSやブラウザがアプリデータ（IndexedDB）を自動削除（クリーンアップ）するのを防ぐ保護機能です。
※本機能を有効化しなくても、データはブラウザ内に常時自動保存されています。

### 使い方
- **「保護を有効化する」と表示されている場合:**
  - タップしてストレージ保護の許可を与えます。
- **「永続化 有効中」と表示されている場合:**
  - 既に保護が有効化されています。特別な操作は不要です。

---

## 3. ☁️ Google Drive クラウド自動同期

### 概要・目的
ご自身の Google アカウントと連携し、ログの記録・修正のたびに全自動でご自身の Google Drive の `TomatoLog` フォルダ内へ日別 Markdown（.md）データおよび添付画像ファイルをリアルタイム保存する機能です。

### 使い方・手順
1. **アプリの設定画面を開く**
   - 画面右上の **「設定 ⚙」** ボタンをタップします。
2. **「Google Drive と連携」ボタンをタップ**
   - **「3. Google Drive クラウド自動同期」** セクションにある **「Google Drive と連携」** ボタン（青色）をタップします。
3. **Google アカウントでログイン & 許可**
   - ポップアップ表示される Google ログイン画面にて、ご自身のアカウントを選択します。
   - アクセス許可確認画面で **「続行」** または **「許可」** を選択します。
4. **連携完了の確認**
   - 設定画面で `連携中 (ユーザーメールアドレス)` のバッジが表示されれば設定完了です。
   - 今後、ログを追加・編集するたびに全自動で Google Drive 上の `TomatoLog/YYYY-MM-DD.md` へ同期保存されます。
   - 過去の全ログを一括で同期したい場合は **「今すぐ手動同期」** をタップします。

### 🔒 セキュリティとプライバシーについて
* **アプリ作成ファイルのみの制限（drive.file スコープ）**:
  本アプリは、ご自身の Google Drive 内の全ファイルを参照する権限は要求しません。`TomatoLog` フォルダ内の本アプリが作成したファイルのみアクセスできる安全な制限です。
* **サーバー非経由ダイレクト通信**:
  認証トークンおよび同期データは、ユーザーのブラウザと Google Drive API 間で直接安全に暗号化通信されます。第三者サーバーを経由することはありません。

---

## 4. 📦 ローカル一括バックアップ

### 概要・目的
これまでアプリ内に保存した日付別の Markdown（.md）ログデータと、添付したすべての画像・ファイルを **1つの ZIP ファイルにまとめてダウンロード** する機能です。

### 使い方・手順
1. 「設定 ⚙」画面内の **「ZIPバックアップ出力」** ボタンをタップします。
2. データ圧縮の進捗状況ダイアログが表示されます。
3. 完了すると、端末に `tomato_log_backup_YYYY-MM-DD.zip` が自動保存されます。

---

## 5. 🔔 タイマー通知の許可

### 概要・目的
設定した時間間隔ごとに、ログの記録を促すアラーム通知をスマホやPCの画面に送信する許可設定です。

### 使い方・手順
1. 「設定 ⚙」画面内の **「通知を許可する」** ボタンをタップします。
2. ブラウザの許可ダイアログで **「許可」** を選択します。
3. トップページのタイマーで「タイマー開始」を押すと、指定時間ごとに「What's up?」の音声とリマインダー通知が届きます。

---

## 6. 📍 位置情報の自動添付

### 概要・目的
ログ保存時に現在地（緯度・経度）を取得し、Google Maps リンクを Markdown 内へ自動添付する機能の ON / OFF 設定です。

### 使い方・手順
1. 「設定 ⚙」画面内の **「位置情報添付 ON / OFF」** ボタンをタップして切り替えます。
2. ON の状態でログを追加すると、自動的に現在地マップリンクが Markdown に記録されます。

---

### お困りの場合（トラブルシューティング）
- **音声が聞こえない場合:** スマホやPCの主音量（マナーモード・ミュート設定）をご確認ください。
- **通知が届かない場合:** スマホ本体の「設定 ➔ 通知 ➔ ブラウザ（Chrome等）」の通知許可がオンになっているかご確認ください。
- **データについて:** データは端末内部（IndexedDB）に保存されるほか、Google Drive 連携を行っている場合はご自身の Google Drive に直接安全に送信・バックアップされます。第三者サーバーを経由することはありません。
