# Tomato Log ユーザー操作マニュアル

Tomato Log（トマトログ）は、スマホやPCのローカル環境のみで安全にデータを管理できるライフログ PWA アプリです。
主要な3つの便利ボタンの使い方と機能について解説します。

---

## 1. 🛡️ 「保護を有効化」/「永続ストレージ」ボタン

### 概要・目的
スマホやブラウザの空き容量が少なくなった際、OSやブラウザが保存データを自動消去してしまうのを防ぎ、データを永続保存するための保護機能です。

### 使い方・表示の変化
- **「保護を有効化」と表示されている場合:**
  1. ボタンをタップします。
  2. ブラウザからストレージ保護の許可が求められたら許可します。
- **「永続ストレージ」と表示されている場合:**
  - 既に保護が有効化されています。特別な操作は不要です。

---

## 2. 📦 「バックアップ」ボタン

### 概要・目的
これまでアプリ内に保存した日付別の Markdown（.md）ログデータと、添付したすべての画像・ファイルを **1つの ZIP ファイルにまとめてダウンロード** する機能です。

### 使い方・手順
1. ヘッダー右上にある **「バックアップ」** ボタンをタップします。
2. データ圧縮の進捗状況ダイアログが表示されます。
3. 完了すると、お使いの端末（ダウンロードフォルダ等）に `tomato_log_backup_YYYY-MM-DD.zip` が自動保存されます。

> **💡 おすすめの活用方法:**
> - 月に1回など定期的にダウンロードしてPCやクラウドストレージへ保管
> - スマホの機種変更時のデータ移行・保存用

---

## 3. 🔔 「通知を許可する」ボタン

### 概要・目的
設定した時間間隔（15分、30分、1時間など）ごとに、ログの記録を促すアラーム通知をスマホやPCの画面に送信する機能です。

### 使い方・手順
1. タイマーカード右上にある **「通知を許可する」** ボタンをタップします。
2. ブラウザに「通知の送信を許可しますか？」というダイアログが出たら **「許可」** を選択します。
3. **「タイマー開始」** ボタンを押すとカウントダウンが始まり、指定時間になると通知が届きます。
4. 届いた通知をタップすると、アプリが開いてスムーズにマイク音声入力や記録が始められます。

---

### お困りの場合（トラブルシューティング）
- **通知が届かない場合:** スマホ本体の「設定 ➔ 通知 ➔ ブラウザ（Chrome等）」の通知許可がオンになっているかご確認ください。
- **データについて:** すべてのデータは端末内部（IndexedDB）にのみ保存されています。外部サーバーへ送信されることはありません。
